# Database Configuration (App)
# Estas credenciales coinciden con la configuración de docker-compose.yml
# Si usas Docker para la base de datos de desarrollo, usa estas credenciales
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=cocos_user
DB_PASSWORD=cocos_password
DB_NAME=cocos
DB_SSL=false

# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration (Tests)
# Estos valores coinciden con la configuración de docker-compose.test.yml
DB_TEST_HOST=localhost
DB_TEST_PORT=5433
DB_TEST_USERNAME=postgres
DB_TEST_PASSWORD=postgres
DB_TEST_NAME=cocos_test
DB_TEST_SSL=false

# Feature Flags
ENABLE_PORTFOLIO_SNAPSHOT=false
